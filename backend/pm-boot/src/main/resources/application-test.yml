spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pm_business_test?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 1

# Test environment logging
logging:
  level:
    com.pm: DEBUG
    org.springframework.security: INFO
